# include "swtch.h"

namespace ns3{

  // void MyReceiveCallback(bool flag, Ptr<NetDevice> device, Ptr<const Packet> packet, uint16_t protocol, const Address& src) {
  //   Mac48Address myaddr = Mac48Address::ConvertFrom(device->GetAddress());
  //   // 处理接收到的数据包
  //   std::cout << "Received packet from " << Mac48Address::ConvertFrom (src) << " to " << myaddr << std::endl;

  // }

}